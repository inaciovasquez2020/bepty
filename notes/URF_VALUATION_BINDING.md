# URF valuation binding

## Bound invariant

Define the URF-side invariant
\[
I_{\mathrm{URF}}(K):=(\#V(K),\#E(K),L_{\deg}(K),I_{cc}(K)).
\]

## Binding map

Define the BEpTy valuation-registry binding
\[
\Psi_{\mathrm{URF}}(K):=I_{\mathrm{URF}}(K).
\]

This realizes one URF invariant inside the BEpTy valuation registry by exact identification with the exact sheaf package.

## Certificate-backed separation theorem

**Theorem.**
Let
\[
K=\Theta(3,3,3),\qquad L=\mathrm{Dumbbell}(6,6).
\]
Then
\[
\beta^{\mathrm{prof}}_{1}(K)\neq \beta^{\mathrm{prof}}_{1}(L),
\]
and
\[
\Psi_{\mathrm{URF}}(K)\neq \Psi_{\mathrm{URF}}(L).
\]

Therefore the witness pair separates both at the profiled-Betti layer and at the exact-package URF-bound valuation layer.

## Registry status

- one URF invariant is bound;
- one certificate-backed separation theorem is fixed;
- executable witness required.
